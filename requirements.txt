# ─────────────────────────────────────────────────────────────────────────────
# LTX-2.5 Runpod Serverless – Python dependencies
#
# Base image: runpod/pytorch:1.1.0-rc.154-cu1281-torch280-ubuntu2204
# (CUDA 12.8.1 + PyTorch 2.8.0 + Python 3.11 + Ubuntu 22.04)
# Do NOT pip-install torch here — it is already in the base image.
# ─────────────────────────────────────────────────────────────────────────────

# ── Runpod SDK ────────────────────────────────────────────────────────────────
runpod>=1.7.3

# ── Hugging Face tooling ──────────────────────────────────────────────────────
huggingface_hub[cli]>=0.23.0
# hf_transfer accelerates large downloads; handler enables it if available
hf_transfer>=0.1.8

# ── Input validation ──────────────────────────────────────────────────────────
pydantic>=2.7.0

# ── Image / video I/O ────────────────────────────────────────────────────────
Pillow>=10.3.0
imageio[ffmpeg]>=2.34.0
# Needed by imageio to write MP4s
imageio-ffmpeg>=0.5.1

# ── LTX-2.5 inference dependencies ───────────────────────────────────────────
# ltx-video is Lightricks' official Python package for LTX models
ltx-video>=0.9.3
# sentencepiece — Gemma tokeniser
sentencepiece>=0.2.0
# transformers — Gemma 4 text encoder + tokeniser wrapper
transformers>=4.50.0
# diffusers — pipeline base (ltx-video builds on top)
diffusers>=0.32.0
# accelerate — device_map / mixed-precision helpers
accelerate>=0.33.0

# ── Numerics ──────────────────────────────────────────────────────────────────
numpy>=1.26.0
einops>=0.8.0

# ── Utilities ────────────────────────────────────────────────────────────────
requests>=2.32.0
tqdm>=4.66.0
loguru>=0.7.2
python-dotenv>=1.0.0
boto3>=1.34.0          # optional: S3-compatible video upload
